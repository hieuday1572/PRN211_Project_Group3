@model PRN211_Project_Group3.Models.ComboSubject

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/5.0.2/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/delete-detail.css">
    <title>Details</title>
</head>
<body>
<div class="color">
        <h1> Delete ComboSubject</h1>
    </div>
<div>

        <div class="container-center">
            <table class="table">
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.ComboId)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Subject)
                    </th>
                </tr>
                <tr>
                    <td>
                        @Html.DisplayFor(model => model.Combo.ComboId)
                    </td>

                    <td>
                        @Html.DisplayFor(model => model.Subject.SubjectCode)
                    </td>
                </tr>
                </table>
            <div class="delete-section">
                <h4>Are you sure you want to delete?</h4>
                <form asp-action="Delete">
                    <input type="hidden" asp-for="ComboId" name="comId" />
                    <input type="submit" value="Delete" class="btn btn-danger" asp-route-id="@Model?.id" class="btn btn-primary" /> |
                    <a asp-action="Details" asp-route-id="@Model?.ComboId" class="btn btn-primary">Back to List</a>
                </form>
            </div>
        
    </div>
</div>
</body>
</html>
